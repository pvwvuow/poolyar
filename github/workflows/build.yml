name: 🚀 Build Poolyar Desktop

on:
  push:
    branches: 
      - main
      - master
      - develop
    tags: 
      - 'v*'
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

env:
  NODE_VERSION: '20'

jobs:
  build-windows:
    name: 🪟 Build Windows
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: 📦 Install Dependencies
        run: |
          if (Test-Path package-lock.json) {
            npm ci --prefer-offline --no-audit 2>&1 | Tee-Object -Variable output
            if ($LASTEXITCODE -ne 0) {
              Write-Host "⚠️ npm ci failed, falling back to npm install..."
              npm install --prefer-offline --no-audit
            }
          } else {
            npm install --prefer-offline --no-audit
          }
        shell: pwsh
      
      - name: 🏷️ Get Version
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            VERSION=$(node -p "require('./package.json').version")
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📌 Version: $VERSION"
      
      - name: 🔨 Build Windows Installer
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 📋 List Build Output
        shell: bash
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Build Output:"
          ls -lh dist/ || echo "No dist folder found"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
      
      - name: ⬆️ Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Poolyar-Windows-v${{ steps.version.outputs.version }}-Build${{ github.run_number }}
          path: |
            dist/Poolyar-Setup-*.exe
            dist/*.blockmap
            dist/latest.yml
          retention-days: 30
          if-no-files-found: error
      
      - name: 🔐 Calculate Checksums
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          cd dist
          for file in Poolyar-Setup-*.exe; do
            if [ -f "$file" ]; then
              sha256sum "$file" > "$file.sha256"
              echo "✅ Checksum: $file"
            fi
          done
      
      - name: 🎉 Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/Poolyar-Setup-*.exe
            dist/Poolyar-Setup-*.exe.sha256
            dist/latest.yml
          draft: false
          prerelease: false
          generate_release_notes: true
          fail_on_unmatched_files: false
          name: Poolyar v${{ steps.version.outputs.version }}
          body: |
            ## 💰 پول‌یار نسخه ${{ steps.version.outputs.version }}
            
            ### 📥 دانلود برای ویندوز
            فایل `Poolyar-Setup-${{ steps.version.outputs.version }}.exe` را از بخش **Assets** پایین صفحه دانلود کنید.
            
            ### 📋 نیازمندی‌های سیستم
            - ✅ ویندوز 10 یا 11 (64 بیتی)
            - ✅ 4GB رم
            - ✅ 200MB فضای خالی دیسک
            
            ### 🔧 نحوه نصب
            1. فایل Setup را دانلود کنید
            2. روی فایل دابل کلیک کنید
            3. مراحل نصب را طی کنید
            4. از برنامه لذت ببرید! 🎉
            
            ### 🌟 ویژگی‌ها
            - رابط کاربری زیبا و کاربردی
            - مدیریت آسان حساب‌ها
            - ذخیره خودکار اطلاعات
            - عملکرد سریع و پایدار
            
            ---
            
            **شماره ساخت:** #${{ github.run_number }} | **کامیت:** [`${{ github.sha }}`](https://github.com/${{ github.repository }}/commit/${{ github.sha }})
            
            ساخته شده با ❤️ توسط pvwvuow
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}