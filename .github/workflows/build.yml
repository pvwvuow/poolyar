name: 🚀 Build Poolyar Desktop

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    name: 🪟 Build for Windows
    runs-on: windows-latest
    
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🏷️ Get Version
        id: version
        shell: bash
        run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: 🔨 Build Windows Installer
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npm run build -- --win

      - name: 📋 List Build Output
        run: dir dist

      - name: ⬆️ Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: poolyar-windows-${{ steps.version.outputs.version }}
          path: |
            dist/*.exe
            dist/*.yml
          retention-days: 30

      - name: 🔐 Calculate Checksums
        shell: pwsh
        run: |
          $exeFile = Get-ChildItem -Path dist -Filter "*.exe" | Select-Object -First 1
          if ($exeFile) {
            $hash = Get-FileHash -Path $exeFile.FullName -Algorithm SHA256
            "$($hash.Hash)  $($exeFile.Name)" | Out-File -FilePath "dist/$($exeFile.BaseName).sha256" -Encoding utf8
            Write-Host "✅ Checksum created for $($exeFile.Name)"
          }

      - name: 🎉 Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.exe
            dist/*.sha256
            dist/latest.yml
          body: |
            # 💰 پول‌یار نسخه ${{ steps.version.outputs.version }}
            
            ## 📥 دانلود
            
            برای **Windows**:
            - دانلود فایل `.exe`
            - دابل کلیک و نصب
            - از برنامه لذت ببرید! 🎉
            
            ## 🔐 امنیت
            
            برای بررسی صحت فایل، checksum SHA256 رو چک کنید.
            
            ## 📝 تغییرات
            
            - 🎉 اولین نسخه پول‌یار
            - ✨ رابط کاربری زیبا و کاربردی
            - 🚀 سرعت بالا با Electron
            
            ---
            
            **ساخته شده با ❤️ توسط تیم پول‌یار**
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}